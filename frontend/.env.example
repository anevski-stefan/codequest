VITE_API_URL=http://localhost:3000

# Run the UI against local fixtures instead of the backend (true/false).
# Useful for UI work without GitHub rate limits or a running server.
VITE_USE_MOCK_DATA=false
# Average simulated response time in ms when mock data is on.
VITE_MOCK_LATENCY=350
